"""按输入长度分档的 prompt 模板。

设计要点（针对 vLLM 前缀缓存）：
- 每档模板的开头句即含 {noun} 变量，首个 16-token block 内即分叉，公共前缀缓存基本失效；
- 变量数量随模板长度增加（短 3 个 → 超长 270+ 个），分散在各资料段落中；
- 每次请求用一个名词填充该档模板的全部变量；
- 尾部统一为「基于以上材料写约 N 字文章」指令；UI 输出长度参数以 token 为单位，
  按 ~1.7 字/token 折算为字数后填入指令。

中文 token 换算按 ~1.7 字/token 估算，各档目标（含指令）：
  极短 ~10 token（沿用旧版一句话指令）
  短   ~100 token ≈ 200 字
  中   ~1k  token ≈ 1700 字
  长   ~8k  token ≈ 13600 字
  超长 ~16k token ≈ 27200 字
"""

# 档位定义：label 用于 UI/报告展示；paras 为拼装的资料段数
# 校准依据：段落库每段（含标题行）约 165 字，中文 ~1.7 字/token
LENGTH_SPECS = {
    "tiny":   {"label": "极短", "paras": 0},
    "short":  {"label": "短",   "paras": 1},
    "medium": {"label": "中",   "paras": 10},
    "long":   {"label": "长",   "paras": 82},
    "xlong":  {"label": "超长", "paras": 165},
}

# 资料段落库：每段约 180-220 字、含 2 个 {noun} 变量。
# 长档/超长档按顺序循环取段（编号不同，内容允许重复——压测关注的是
# token 规模与前缀分叉，而非语料唯一性）。
_TOPIC_PARAS = [
    ("技术架构",
     "从技术架构来看，{noun}相关的系统在近十年经历了多次重要演进。早期方案受限于单机算力与存储容量，"
     "只能在独立节点上运行，扩展性十分有限；随着分布式框架与云原生技术成熟，主流实现已可在集群环境中"
     "横向扩容，并借助多层缓存与批处理机制大幅降低响应延迟。工程团队围绕{noun}做选型时，通常会综合"
     "评估吞吐量、一致性模型与运维成本三个维度，不同权重会导向截然不同的技术路线。"),
    ("市场分析",
     "市场研究机构对{noun}相关领域的判断总体乐观，但节奏预期存在分歧。乐观派认为需求端已经跨过临界点，"
     "未来数年将保持较高复合增速；谨慎派则强调商业化路径尚不清晰，早期订单多来自概念验证项目，规模化"
     "复制仍需时间验证。从产业链调研看，围绕{noun}的预算正在从创新实验向核心业务系统迁移，这一结构性"
     "变化往往预示着采购决策权的转移与客单价的抬升。"),
    ("应用场景",
     "在应用层面，{noun}已渗透到多个行业的关键流程之中。金融领域用它处理风控与反欺诈场景，对延迟与"
     "准确率的要求极为苛刻；制造领域将其嵌入预测性维护体系，结合传感器数据提前识别设备异常；医疗领域"
     "则聚焦辅助诊断与病历结构化，需要格外关注合规边界。不同场景对{noun}的能力诉求差异显著，通用方案"
     "往往需要深度定制才能真正落地。"),
    ("历史沿革",
     "回顾{noun}的发展脉络，可以大致划分为三个阶段。第一阶段以学术探索为主，核心成果集中在理论与"
     "原型验证；第二阶段工程化成为主线，可用性与成本问题逐步得到解决；当前所处的第三阶段，竞争焦点"
     "转向规模化落地与生态构建。每一次阶段跃迁都伴随着基础设施的代际更新，而{noun}的形态也在这个"
     "过程中不断被重新定义。"),
    ("未来趋势",
     "展望未来，围绕{noun}的技术演进将呈现几条清晰主线：其一是效率革命，推理成本有望在算法与芯片的"
     "双重推动下持续下探；其二是多模态融合，单一文本能力将扩展为跨模态理解与生成；其三是端侧部署，"
     "轻量化模型使{noun}能力下沉到手机与边缘设备成为可能。这些趋势相互叠加，可能在未来数年内重塑"
     "整个产业格局。"),
    ("挑战与风险",
     "尽管前景广阔，{noun}的落地仍面临不容忽视的挑战。数据层面，高质量语料的获取与合规使用始终是"
     "绕不开的难题；工程层面，长尾场景的稳定性与可解释性远未达到关键业务的要求；组织层面，人才缺口"
     "与流程再造的隐性成本常被低估。多位从业者提醒，对{noun}的投入应当与业务成熟度匹配，激进路线"
     "往往在规模化阶段暴露系统性风险。"),
    ("产业生态",
     "产业生态方面，{noun}已形成层次分明的分工体系。上游是芯片与基础设施供应商，提供算力底座；中游"
     "是平台与工具厂商，负责模型训练、部署与监控；下游则是面向垂直行业的应用服务商。生态位之间的"
     "边界正在模糊化，头部厂商持续向上下游延伸，围绕{noun}的合纵连横日趋频繁，标准之争或将成为下一"
     "阶段的焦点。"),
    ("政策环境",
     "政策层面，各主要经济体对{noun}相关技术的监管框架正加速成型。一方面，产业扶持政策密集出台，"
     "从资金、算力到数据要素多管齐下；另一方面，安全评估、内容合规与算法备案的要求也在收紧。合规"
     "成本正在成为企业布局{noun}时必须纳入考量的固定投入，提前建立治理体系的企业将在监管周期中"
     "占据主动。"),
    ("学术研究",
     "学术界的关注点正在从{noun}的能力上限转向效率与可靠性。顶会论文中，模型压缩、投机解码与缓存"
     "优化的占比逐年攀升；评测方法学也引发广泛讨论——静态基准与真实场景表现之间的相关性屡遭质疑。"
     "多个团队开始发布面向{noun}的动态评测集，试图以持续更新的任务流衡量系统的泛化能力，这一方向"
     "被认为更具实践指导意义。"),
    ("商业模式",
     "商业模式上，围绕{noun}的变现路径逐渐清晰。订阅制适合能力持续迭代的标准产品；按量计费匹配"
     "波动明显的业务负载；私有化部署则满足数据敏感型客户的需求。值得关注的是，部分厂商尝试将"
     "{noun}能力封装为行业解决方案，以项目制切入后再转为订阅，这种「先服务后产品」的策略在当前"
     "市场环境下显示出更高的签约成功率。"),
    ("用户体验",
     "从用户视角看，{noun}类产品的体验门槛远比想象中高。响应速度直接影响留存——首屏反馈超过数秒"
     "便会出现明显流失；输出质量的稳定性同样关键，偶发的低质回答对信任的破坏远超优质回答带来的"
     "增益。领先团队开始引入人机协同设计，让{noun}在不确定时主动降级或求助，而非强行给出答案。"),
    ("安全合规",
     "安全与合规是{noun}规模化部署的前置条件。提示注入、数据泄露与幻觉控制构成三大技术挑战，"
     "行业已沉淀出输入过滤、输出校验与溯源水印的组合防线；法律层面，训练数据版权、生成内容责任"
     "归属等议题仍存争议。多数企业选择以「可审计」为原则构建{noun}的治理流程，确保每一次关键"
     "决策都能回溯与解释。"),
    ("成本结构",
     "成本结构决定了{noun}业务的可持续性。算力支出通常占据总成本的大头，但隐性成本同样不容小觑："
     "数据清洗与标注的人力投入、推理链路的工程维护、以及失败重试带来的资源浪费。精细化运营团队"
     "会为{noun}建立完整的单位经济模型，从单次调用成本追踪到客户生命周期价值，以此指导定价与"
     "资源调度策略。"),
    ("竞争格局",
     "竞争格局方面，{noun}赛道已从蓝海转入淘汰赛。头部厂商凭借资金与数据优势持续扩大领先，"
     "中部玩家则在垂直领域构筑差异化壁垒，长尾团队面临出清压力。一个反复被验证的规律是：技术"
     "领先窗口期正在缩短，围绕{noun}的护城河更多来自数据飞轮、客户切换成本与生态锁定，而非"
     "单点算法优势。"),
    ("人才供给",
     "人才市场对{noun}相关岗位的需求经历了从狂热到理性的回归。早期高薪抢人的局面有所缓解，但"
     "具备工程落地能力的复合型人才依然稀缺——既理解模型原理，又能处理分布式部署、性能调优与"
     "成本控制的候选人往往一人难求。高校课程体系正在快速跟进，企业内训与认证机制逐渐成为{noun}"
     "人才供给的重要补充渠道。"),
]

# 短档专用段落（~110 字）：整档合计约 170 字 ≈ 100 token
_SHORT_PARA = ("【资料 1·领域动态】在{noun}领域，近期最受关注的进展集中在工程化落地环节。"
               "多家机构发布的评估报告显示，核心性能指标在过去一年均有明显改善，"
               "但距离大规模商用仍存在差距。业内人士普遍认为，{noun}的下一阶段"
               "竞争将围绕成本与可靠性展开。")

# 开头句：变量位于前几个 token，确保首个缓存 block 即分叉
_HEADER = '以下是关于"{noun}"的专题资料汇编。'

# 尾部指令：与「输出长度（token）」参数衔接（token×1.7 折算为字数），并强调字数遵循
_TAIL = ("请基于以上材料，写一篇约{N}字的文章，写作时请严格遵循该字数要求，"
         "直接输出文章正文。")

# 极短档：沿用旧版一句话指令（用户确认）
_TINY = '围绕"{noun}"写一篇{N}字的文章。直接输出文章正文。'


def _build_body(para_count: int) -> str:
    """按段数拼装资料正文：段落循环取自段落库，带【编号·主题】标题行。"""
    parts = []
    for i in range(para_count):
        topic, text = _TOPIC_PARAS[i % len(_TOPIC_PARAS)]
        parts.append(f"【资料 {i + 1}·{topic}】{text}")
    return "\n\n".join(parts)


# 模块加载时预构建各档正文（静态内容，进程内复用）
_BODIES = {key: _build_body(spec["paras"]) for key, spec in LENGTH_SPECS.items()}


def build_prompt(input_length: str, noun: str, output_tokens: int) -> str:
    """按档位生成完整输入 prompt：名词填充该档模板的全部变量。

    output_tokens 为目标输出 token 数，按 ~1.7 字/token 折算为字数写入指令
    （模型对「字数」的遵循远好于「token 数」）。
    """
    if input_length not in LENGTH_SPECS:
        input_length = "tiny"
    n_chars = max(20, round(output_tokens * 1.7))
    if input_length == "tiny":
        return _TINY.format(noun=noun, N=n_chars)
    if input_length == "short":
        return (_HEADER + "\n\n" + _SHORT_PARA + "\n\n" +
                _TAIL.format(N=n_chars)).replace("{noun}", noun)
    return (_HEADER + "\n\n" + _BODIES[input_length] + "\n\n" +
            _TAIL.format(N=n_chars)).replace("{noun}", noun)


def truncate_for_store(text: str, head: int = 300, tail: int = 200) -> str:
    """问答记录存储用截断：超长 prompt 只保留开头+结尾，中间以省略标记替代。"""
    if len(text) <= head + tail + 50:   # 略长于阈值时原样保留
        return text
    omitted = len(text) - head - tail
    return (text[:head] +
            f"\n\n……[中间 {omitted} 字已省略，仅存储首尾部分]……\n\n" +
            text[-tail:])
